@model IEnumerable<AllShow.Models.edmx.SHCLASS>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.shclassname)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.shclassname)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.shclassno }) |
            @Html.ActionLink("Details", "Details", new { id=item.shclassno }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.shclassno })
        </td>
    </tr>
}

</table>
