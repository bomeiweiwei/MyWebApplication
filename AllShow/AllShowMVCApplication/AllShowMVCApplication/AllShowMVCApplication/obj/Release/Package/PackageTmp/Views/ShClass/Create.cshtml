@model AllShowMVCApplication.Models.SHCLASS

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>SHCLASS</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.shclassno)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.shclassno)
            @Html.ValidationMessageFor(model => model.shclassno)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.shclassname)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.shclassname)
            @Html.ValidationMessageFor(model => model.shclassname)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
