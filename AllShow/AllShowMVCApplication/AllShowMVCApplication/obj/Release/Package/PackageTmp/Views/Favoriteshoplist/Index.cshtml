@model IEnumerable<AllShowMVCApplication.Models.favoriteshoplist.Favoriteshoplist>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model=>model.memNo)
        </th>
        <th>
            @Html.DisplayNameFor(model=>model.shNo)
        </th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink("Edit", "Edit", new {  id=item.memNo ,id2=item.shNo  }) |
            @Html.ActionLink("Details", "Details", new {  id=item.memNo ,id2=item.shNo  }) |
            @Html.ActionLink("Delete", "Delete", new {  id=item.memNo ,id2=item.shNo  })
        </td>
    </tr>
}

</table>
