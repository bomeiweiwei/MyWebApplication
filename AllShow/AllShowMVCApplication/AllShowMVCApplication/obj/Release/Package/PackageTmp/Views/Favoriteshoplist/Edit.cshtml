@model AllShowMVCApplication.Models.favoriteshoplist.Favoriteshoplist

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Favoriteshoplist</legend>

        @Html.HiddenFor(model => model.memNo)

        @Html.HiddenFor(model => model.shNo)

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
